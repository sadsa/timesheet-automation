{
  "name": "timesheet-automation",
  "version": "1.0.0",
  "description": "Submit timesheet entries to the Entelect portal from GitHub-derived activity",
  "main": "src/timesheet-submit.js",
  "type": "module",
  "scripts": {
    "submit": "node src/timesheet-submit.js",
    "test": "node --test"
  },
  "dependencies": {
    "chalk": "^5.0.0",
    "playwright": "^1.40.0"
  }
}
